"""IAM Admin Interface

Admin interface for IAM models.
Currently references accounts app models.
"""

from django.contrib import admin

# When models are moved to core.iam in Phase 3:
# from .models import User, Aplicacao, Role, UserRole, Attribute, RolePermission

# For now, admin is still in accounts app
# This file is placeholder for future migration